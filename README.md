<h1>Nuliz - nulis ini nulis itu!</h1>
Minimum PHP 8.2 to run Laravel 12. I use the older versions because my other projects used it too. But Laravel 12 supports PHP 8.5.
run php artisan migrate to create the database tables. You can also run php artisan db:seed to fill the database with dummy data. The dummy data is created using Faker library, so you can customize it in the DatabaseSeeder.php file.